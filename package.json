{
  "name": "claude-session-analyser",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first cost analytics, token-saving insights and ML cost forecasting for Claude Code sessions",
  "type": "module",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c yellow,cyan \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "start": "npm run dev",
    "test": "npm test --workspace server && npm test --workspace client",
    "coverage": "npm run coverage --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "license": "MIT"
}
