{
  "name": "@claude-session-analyser/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=../.env src/index.js",
    "start": "node --env-file-if-exists=../.env src/index.js",
    "test": "node --test",
    "coverage": "c8 --include \"src/**\" --reporter text --reporter text-summary node --test"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "c8": "^10.1.3"
  }
}
